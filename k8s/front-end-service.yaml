apiVersion: v1
kind: Service

metadata:
  name: todo-app
  namespace: todo

spec:
  selector:
    app: todo-app
    
  type: NodePort
    
  ports:
    - port: 80
      targetPort: 80
      nodePort: 30080
